**How to compile a file:**  
```
lua src/main.lua FILE_NAME
```

**Syntax example:**   
```
0 -> var num;
2 -> const first;
4 -> const second;

{
    return a * b;
}[a, b] -> const multiply;

first, second -> multiply -> num;

{
    num -> print;
} -> loop;
```